# Ensure all platform-specific binaries are installed
# This is crucial for lightningcss to work on different platforms during deployment
optional=false
platform-specific=true

# Force npm to install all optional dependencies
include=optional

# Ensure binaries are rebuilt for the target platform
rebuild=true